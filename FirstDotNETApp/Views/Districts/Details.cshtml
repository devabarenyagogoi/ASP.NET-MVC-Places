@model FirstDotNETApp.ViewModels.DistrictViewModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div class="card-custom">
    <h4>District</h4>
    <hr />

    <dl class="row mb-0">
        <dt class="col-sm-3">
            District ID
        </dt>
        <dd class="col-sm-9">
            @Model.DistrictId
        </dd>

        <dt class="col-sm-3">
            District Name
        </dt>
        <dd class="col-sm-9">
            @Model.DistrictName
        </dd>

        <dt class="col-sm-3">
            State Name
        </dt>
        <dd class="col-sm-9">
            @Model.StateName
        </dd>
    </dl>

    <hr />

    <div class="d-flex gap-2">
        <a asp-action="Edit" asp-route-id="@Model.DistrictId" class="btn btn-primary">
            Edit
        </a>

        <a asp-action="Index" class="btn btn-secondary">
            Back
        </a>
    </div>
</div>